development:
  driver: postgres
  open: "postgres://mnemo_user:mnemo_password@localhost:5432/mnemo_db?sslmode=disable"
# Add other environments like 'test' or 'production' if needed
# production:
# driver: postgres
# open: ${DATABASE_URL} # Example using an environment variable
